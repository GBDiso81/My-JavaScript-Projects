function myFunction() {
    var sentence = "I am learning"; //assigning the variable
    sentence += " a lot from this book! "; //using operators to add assigned variables
    document.getElementById("concatenate").innerHTML = sentence; //using getelementbyID
}